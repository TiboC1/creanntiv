{% extends "_layout.twig" %}

{% block content %}

<!-- Page Header -->
  <header class="masthead" style="background-image: url('{{alias('@web')}}/assets/banners/index.JPG')">
    <div class="overlay"></div>
    <div class="container">
      <div class="row">
        <div class="col-lg-8 col-md-10 mx-auto">
          <div class="site-heading">
            <h1>Creanntiv</h1>
            <span class="subheading">Make something extra- out of the ordinary</span>
          </div>
        </div>
      </div>
    </div>
  </header>

  <!-- Main Content -->
  <div class="container">
    <div class="row">
      <div class="col-lg-8 col-md-10 mx-auto">
        <h2 class="h1">Latest Blog Posts</h2>
        <hr>
        {% for post in craft.entries().section('blog').limit(5) %}
          <div class="post-preview">
            <a href="{{ post.url }}">
              <h2 class="post-title">
                {{ post.title }}
              </h2>
              {% if post.blogImage.one() %}
                <img src="{{ post.blogImage.one().url() }}" style="max-height: 500px;" class="img-fluid my-3">
              {% endif %}
              <blockquote class="blockquote">
                "{{ post.body|slice(0,20) }}
                {% if post.body|length > 20 %}
                <span>...</span>
                {% endif %}
                "
              </blockquote>
            </a>
            <p class="text-muted my-1" >Posted on {{ post.postDate | date("M d, Y") }}</p>
            <hr>
          </div>
        {% endfor %}
        <a href="{{alias('@web')}}/en/blog" class="btn btn-dark">View All Posts</a>
      </div>
    </div>
  </div>

{% endblock %}